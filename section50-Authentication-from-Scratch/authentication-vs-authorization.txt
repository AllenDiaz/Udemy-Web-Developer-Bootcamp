Authentication - verify who's user
Authorization - verify what use access to 

### **Cryptographic Hash Functions: Overview & Applications**

A **cryptographic hash function (CHF)** is a mathematical algorithm that transforms input data into a fixed-length hash value. It plays a crucial role in **data integrity, authentication, and security**.

---

### **Key Properties of Cryptographic Hash Functions**

✅ **Deterministic** – The same input always produces the same hash.

✅ **Fixed-Length Output** – Regardless of input size, the hash has a consistent length.

✅ **Collision Resistance** – No two different inputs should produce the same hash.

✅ **Pre-Image Resistance** – It should be computationally infeasible to reverse-engineer the original input from the hash.

✅ **Avalanche Effect** – A small change in input drastically alters the hash output.

---

### **Common Cryptographic Hash Algorithms**

🔹 **MD5** – Fast but vulnerable to collisions.

🔹 **SHA-256** – Secure and widely used in blockchain and authentication.

🔹 **SHA-3** – A newer standard with enhanced security.

🔹 **BLAKE2** – Optimized for speed and security.

---

### **Applications of Cryptographic Hash Functions**

🔹 **Password Storage** – Hashing passwords before storing them prevents direct exposure.

🔹 **Digital Signatures** – Ensures message authenticity and integrity.

🔹 **Blockchain & Cryptocurrency** – Used in Bitcoin and Ethereum for transaction verification.

🔹 **Data Integrity Checks** – Verifies that files or messages haven't been altered.

🔹 **Message Authentication Codes (MACs)** – Used for secure communication.